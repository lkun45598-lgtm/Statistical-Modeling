[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scs-sst-mhw-risk"
version = "0.1.0"
description = "South China Sea SST warming and marine heatwave risk assessment workflow"
requires-python = ">=3.10"
authors = [{name = "Leizheng"}]
